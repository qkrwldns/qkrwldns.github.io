---
title: python 공부 2024.03.19
author: cotes
date: 2024-03-19 17:33:00 +0800
categories: [폴리텍, python]
tags: [python]
pin: true
math: true
mermaid: true
image:
  path: ./assets/img/posts/python-logo.png
  lqip: data:image/webp;base64,UklGRpoAAABXRUJQVlA4WAoAAAAQAAAADwAABwAAQUxQSDIAAAARL0AmbZurmr57yyIiqE8oiG0bejIYEQTgqiDA9vqnsUSI6H+oAERp2HZ65qP/VIAWAFZQOCBCAAAA8AEAnQEqEAAIAAVAfCWkAALp8sF8rgRgAP7o9FDvMCkMde9PK7euH5M1m6VWoDXf2FkP3BqV0ZYbO6NA/VFIAAAA
  alt: 파이썬 로고 img 
---

# 파이썬의 강점과 활용

파이썬의 위치는 딥러닝, 간편한 학습 곡선, 그리고 풍부한 라이브러리 덕분에 급격히 상승했습니다. 이는 파이썬을 데이터 과학, 웹 개발, 자동화 등 다양한 분야에서 인기 있는 선택으로 만들었습니다.

## 파이썬의 특징
- 스크립트 언어: 컴파일 언어에 비해 실행 속도가 느릴 수 있지만, 개발 속도는 빠릅니다.
- 하드웨어 제어에 제약: 하드웨어 수준의 제어가 필요한 경우 다른 언어로 넘어가야 할 수도 있습니다.
- 데이터 분석 라이브러리: Pandas, NumPy, Matplotlib와 같은 라이브러리는 데이터 분석을 쉽고 효율적으로 만듭니다.

## 데이터의 형태
- 정형 데이터: 엑셀 파일, 데이터베이스 파일 등, 정해진 형식에 따라 구성된 데이터.
- 비정형 데이터: 이미지, 비디오, 오디오 파일 등, 사이즈가 크고 정형화되지 않은 데이터.

## 교육적 활용
파이썬은 과학 계산 기능과 다양한 오픈소스 라이브러리로 인해 교육 분야에서도 널리 사용됩니다.

## 개발 환경
- Google Colab: 온라인 Jupyter Notebook 환경을 무료로 제공.
- Visual Studio Code: 다양한 언어를 지원하는 경량 에디터.
- Jupyter Notebook: 데이터 과학 프로젝트에 널리 사용되는 웹 애플리케이션.
- PyCharm: 파이썬 전용 IDE로 다양한 기능을 제공하지만 상대적으로 무겁습니다.

## 운영 체제와 도메인
- 운영 체제 점유율: 한 때 Windows의 점유율은 97~98%에 달했지만, macOS와 리눅스의 인기 상승으로 인해 변화가 생겼습니다.
- 도메인 명명 규칙: .org는 비영리, .com은 상업적 목적, .kr은 한국, .jp은 일본을 나타냅니다.

## 프로그래밍 팁
- 파이썬 설치: VS Code나 Jupyter Notebook을 통해 파이썬 설치가 가능합니다. 설치 시 경로를 잘 체크해야 합니다.
- 라이브러리 설치: pip 명령어를 통해 필요한 라이브러리를 설치할 수 있습니다.

## 기본 문법과 예제
파이썬의 기본 문법을 익히는 것은 프로그래밍 기초를 다지는 데 중요합니다. 문자열 포맷팅, 논리 연산자, 조건문 등은 파이썬 프로그래밍에서 자주 사용되는 구문입니다.

<예약어>

%d %x %o 정수(10,16,8진수) 

%f 실수

%c 한글자

%s 두글자 이상 문자열 

파이썬에서 정수형(int)의 범위는 사실상 메모리의 한계에 의해서만 제한됩니다. 이는 파이썬 3부터 적용된 변경 사항으로, 파이썬 2에서는 `int`와 `long` 타입이 별도로 존재했으며, `int`의 크기는 시스템에 따라 32비트 또는 64비트였습니다. 하지만 파이썬 3에서는 `int`가 `long`을 흡수하며 사실상 무한대의 크기를 가질 수 있게 되었고, 이는 사용 가능한 시스템 메모리에 의해서만 제한됩니다.

따라서 파이썬 3에서는 아주 큰 수나 아주 작은 음수도 `int` 타입으로 표현할 수 있습니다. 예를 들어, 다음과 같이 매우 큰 수를 변수에 할당할 수 있습니다:

```python
## 큰따옴표 안의 모든건 str 문장으로 인식
print("안녕하세요?")
# 문자
print("100")
# 숫자
print("%d" %100)

# ------------------------------------------------------------
## 에러예제
print("%d %d" %(100))
print("%d" % 100 100)
# 파라미터 두 개 받는데 하나만 줘도 에러나고 파라미터 한 개 받는데 두개를 줘도 에러남 

# ------------------------------------------------------------
## 변수 타입
# 정수
print("%d" %(123))
print("%5d" %(123))
print("%05d" %(123))
# 실수
print("%f" %(123.45))
print("%7.1f" %(123.45))
print("%7.3f" %(123.45))
# 문자열
print("%s" %("python"))
print("%10s" %("python"))

# ------------------------------------------------------------
# 띄우기
print("한 행입니다. 또 한 행입니다.")
print("한 행입니다. \n또 한 행입니다.")

# ------------------------------------------------------------
## 문자열 포맷팅
print("Hello, %s" % "World")
print("Number: %d" % 100)
print("{2:d} {1:d} {0:d}".format(100, 200, 300))
# print("{2:d} {1:d} {0:d}".format(100, 200, 300)) 이건 300이 2번쩨 자리이고 첫번쩨에 2번쨰잇는 자리를 출력한다는것

# ------------------------------------------------------------
## 논리 연산자
a = 10
print(a > 5 and a < 15)  # True
print(a > 15 or a < 12)  # True
print(not(a > 15))       # True

## 조건문
if a < 10:
    print("Less than 10")
else:
    print("10 or more")

# ------------------------------------------------------------
# 사칙연산
a: int = 10; b: int = 20
print(a + b, a - b, a * b, a /b)
#  ** 제곱, % 나머지를 구함, // 나눈 후에 소수점을 버림

# ------------------------------------------------------------
# 불형
# true, false 논리형이라고도 함
a: bool = (100 == 100)
b: bool = (10 > 100)
print(a, b)

# ------------------------------------------------------------
# 문자열
a: str = "파이썬 만세"
print(a)
print(type(a))

# ------------------------------------------------------------
# 문자열과 숫자의 상호 변환
s1, s2, s3 = "100", "100.123", "99999999999999999"
print(int(s1) + 1, float(s2) + 1, int(s3) + 1)

# 문자열과 숫자의 상호 변환
a = 100; b = 100.123
print(str(a) + '1');print(str(b) + '1')

# ------------------------------------------------------------
# 대입연산자
a: int = 10
a += 5; print(a)
a -= 5; print(a)
a *= 5; print(a)
a /= 5; print(a)
a //= 5; print(a)
a %= 5; print(a)
a **= 5; print(a)

# ------------------------------------------------------------
# 관계 연산자
a, b = 100, 200
print(a == b, a != b, a > b, a < b, a >= b, a <= b)

# ------------------------------------------------------------
# 논리 연산자
a = 99
print((a > 100) and (a < 200))
print((a > 100) or (a < 200))
print(not (a == 100))

# ------------------------------------------------------------
# 조건문 if
a = 200
if a < 100:
    print("100보다 작군요.")
    print("거짓이므로 이 문장은 안 보이겠죠?")
print("프로그램 끝")

# 조건문 if , else
a = 99
if a < 100:
    print("100보다 작군요.")
else:
    print("100보다 크군요.")

a = int(input("정수를 입력하세요: "))
if a % 2 == 0:
    print("%d 짝수를 입력했군요." %a)
else:
    print("%d 홀수를 입력했군요." %a)
```

### **`and` 연산자**

- **`A and B`**는 두 조건 **`A`**, **`B`**가 모두 참일 때만 참을 반환합니다.
- **`A`**가 거짓이면 **`A`**의 값을 반환하고, 그렇지 않으면 **`B`**의 값을 반환합니다.
- 논리 연산에서 모든 값은 참 또는 거짓으로 평가됩니다. **`0`**, **`None`**, 빈 컬렉션(**`[]`**, **`{}`**, **`''`** 등)은 거짓으로 평가됩니다.

### **`or` 연산자**

- **`A or B`**는 두 조건 **`A`**, **`B`** 중 하나라도 참이면 참을 반환합니다.
- **`A`**가 참이면 **`A`**의 값을 반환하고, 그렇지 않으면 **`B`**의 값을 반환합니다.
- **`or`** 연산자 역시 모든 값이 참 또는 거짓으로 평가됩니다.

### **`not` 연산자**

- **`not A`**는 조건 **`A`**의 논리적 반대를 반환합니다.
- **`A`**가 참이면 **`False`**를, 거짓이면 **`True`**를 반환합니다.

예시 
```python
print(True and False)  # False
print(False or True)   # True
print(not True)        # False

# 0은 거짓으로 평가됩니다.
print(0 and 2)  # 0

# 빈 문자열('')은 거짓으로 평가되고, 'python'은 참으로 평가됩니다.
print('' or 'python')  # python

# not 연산자는 참을 거짓으로, 거짓을 참으로 반전시킵니다.
print(not '')  # True

```

논리 연산자는 조건문, 반복문 등 다양한 곳에서 조건을 결합하거나 반전시키는 데 사용됩니다.

not 연산자는 단항 연산자로, 한 번에 하나의 조건 또는 표현식에만 적용됩니다. 두 변수에 대해 not 연산을 동시에 수행하고자 한다면, 각 변수에 대해 개별적으로 not 연산을 적용하거나, 두 변수를 논리 연산자(and, or)로 결합한 후 전체 표현식에 not을 적용해야 합니다.

## 동전 교환 프로그램 예제
동전 교환 프로그램은 사용자가 입력한 금액을 동전으로 교환해주는 간단한 프로그램입니다. 이 예제는 파이썬의 기본적인 입력 처리, 산술 연산, 조건문을 활용합니다.

```python
# 사용자로부터 교환할 금액 입력 받기
money = int(input("교환할 돈은 얼마? "))

# 각 동전으로 교환
c500 = money // 500
money %= 500

c100 = money // 100
money %= 100

c50 = money // 50
money %= 50

c10 = money // 10
money %= 10

# 결과 출력
print("500원짜리 => %d개" % c500)
print("100원짜리 => %d개" % c100)
print("50원짜리 => %d개" % c50)
print("10원짜리 => %d개" % c10)
print("바꾸지 못한 잔돈 => %d원" % money)
```

